# Copy this file to ".env" in the project root and fill in your keys.
# PhoneINT works without these — the plugins that need them will just
# report [skipped] instead of failing.

HIBP_API_KEY=
ABSTRACTAPI_PHONE_KEY=
